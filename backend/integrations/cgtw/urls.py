from django.urls import path

# 后续会添加同步视图
# from .views import ManualSyncView, SyncLogsView

urlpatterns = [
    # path('manual/', ManualSyncView.as_view(), name='manual_sync'),
    # path('logs/', SyncLogsView.as_view(), name='sync_logs'),
] 